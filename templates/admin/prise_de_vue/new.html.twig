{% extends 'base.html.twig' %}
{% import 'macros/forms.html.twig' as forms %}
{% import 'macros/messages.html.twig' as messages %}

{% block title %}Nouvelle prise de vue - Studio Prunelle{% endblock %}

{% block body %}
<div class="container mt-4">
    {{ forms.breadcrumb([
        {'label': 'Accueil', 'path': 'home'},
        {'label': 'Prises de vue', 'path': 'admin_prise_de_vue_index'},
        {'label': 'Nouvelle prise de vue'}
    ]) }}

    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1><i class="bi bi-camera me-2"></i>Créer une nouvelle prise de vue</h1>
        <a href="{{ path('admin_prise_de_vue_index') }}" class="btn btn-outline-secondary">
            <i class="bi bi-arrow-left"></i> Retour à la liste
        </a>
    </div>

    {{ messages.flash_messages() }}
    {{ messages.form_errors_summary(form) }}

    {{ include('admin/prise_de_vue/_form.html.twig') }}
</div>
{% endblock %}